# AGENTS.md

## Cursor Cloud specific instructions

This repository is a static document store for personal PDF certificates (Polish: "Certyfikaty"). It contains no source code, no dependencies, no build system, and no runnable services.

- **No dependencies** to install (no `package.json`, `requirements.txt`, etc.)
- **No lint, test, or build** commands available
- **No services** to start or test
- Repository contents: `README.md` and two PDF certificate files
